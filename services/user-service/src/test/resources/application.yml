spring:
  datasource:
    url: jdbc:h2:mem:users;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
app:
  security:
    jwt-secret: test-secret-that-is-at-least-thirty-two-bytes-long
    access-token-ttl: PT15M
  bootstrap-admin:
    email: ""
    password: ""
    name: Test Administrator
