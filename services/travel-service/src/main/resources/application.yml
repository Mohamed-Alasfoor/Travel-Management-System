spring:
  config:
    import: ${VAULT_CONFIG_IMPORT:optional:classpath:/vault-disabled.properties}
  application:
    name: travel-service
  cloud:
    vault:
      enabled: ${VAULT_ENABLED:false}
      uri: ${VAULT_URI:http://localhost:8200}
      token: ${VAULT_TOKEN:test-disabled-token}
      kv:
        enabled: true
        application-name: travel-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/travels_db}
    username: ${DB_USERNAME:travels_app}
    password: ${DB_PASSWORD:travels_dev_only}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
server:
  address: 0.0.0.0
  port: ${SERVER_PORT:8082}
app:
  security:
    jwt-secret: ${JWT_SECRET:dev-only-secret-must-be-at-least-32-bytes-long}
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
